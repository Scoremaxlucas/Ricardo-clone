# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/
build
dist/

# Production
*.log
*.pid
*.seed

# Environment variables
# Ignore .env file and all .env.* files, then explicitly allow .env.example
.env
# Ignore all .env.* files first
.env.*
# Then allow .env.example (negation must come after general rule to override it)
!.env.example

# Database
prisma/*.db
prisma/*.db-journal

# IDE
# .vscode/ - Nicht ignorieren, damit Workspace-Einstellungen geteilt werden können
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# LaunchAgent plist files (use templates instead)
# Ignore all plist files first
*.plist
# Then allow plist template files (negation must come after general rule to override it)
!*.plist.template

# Expo
.expo/
.expo-shared/

# Mobile app
mobile-app/node_modules/
mobile-app/.expo/
.vercel
.env*.local
