# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/
build
dist/

# Production
*.log
*.pid
*.seed

# Environment variables
# Ignore .env file and all .env.* files, then explicitly allow .env.example
.env
# Allow .env.example first (negation must come before general rule)
!.env.example
# Then ignore all other .env.* files
.env.*

# Database
prisma/*.db
prisma/*.db-journal

# IDE
# .vscode/ - Nicht ignorieren, damit Workspace-Einstellungen geteilt werden können
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# LaunchAgent plist files (use templates instead)
# Allow all plist template files first (negation must come before general rule)
!*.plist.template
# Then ignore all other plist files
*.plist

# Expo
.expo/
.expo-shared/

# Mobile app
mobile-app/node_modules/
mobile-app/.expo/
